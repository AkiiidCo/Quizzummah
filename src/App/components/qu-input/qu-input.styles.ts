import styled from 'styled-components';
import { GlobalColors } from '../../styles/global-styles';
import { btn_default, input_default, row_center_both } from '../../styles/styles-helpers';
export const QUInputContainer = styled.div``;
export const QULabel = styled.div`
	text-transform: capitalize;
	color: ${GlobalColors.darkBlue};
`;
export const QUInputField = styled.input`
	${input_default}
`;
